version: "3.9"

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vendorguard
      POSTGRES_PASSWORD: vendorguard
      POSTGRES_DB: vendorguard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vendorguard"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+psycopg2://vendorguard:vendorguard@db:5432/vendorguard
      SECRET_KEY: vendorguard-dev-secret-key-change-in-production
      AI_ENABLED: "false"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./reports:/app/reports

volumes:
  pgdata:
