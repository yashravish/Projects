# Provisioning example: add Prometheus in Grafana, point to your Prometheus in Docker/Compose.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
