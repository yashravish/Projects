// This code runs on a dynamic page connected to the "Artists" collection.
$w.onReady(function () {
    // The dataset (e.g., #datasetArtists) automatically loads data based on the URL parameter
    // Ensure your page elements are bound to the dataset fields (e.g., #artistName, #artistBio, #artistPortrait)
  });
  